Object:

Room
Desk
User

Queue
MessageQueue
Message(from, to, content, time)
RequestQueue
Request

RequestHandler(Request request) implements Callable {
	//构造一个Request接口的具体对象
	//放到队列
}

RequestExecutor {
	//合法性验证, 执行的时候，先执行if(Request.isExecutable()){
		
	}
}

Watcher

