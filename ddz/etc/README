Original_II.rar
是之前我做的一些架构，主要是调整了HTTPCONNECT为异步模式，
服务器也做了相应修改。

也就是每一次客户端对服务端发起请求，不是马上得到结果，而是先获得一个以前的结果消息(Message)，
然后服务端再对本次请求做处理，放到消息（Message）队列中，下次客户端再请求就可以获取了。

具体到客户端，需要实现XMidlet类的update方法，对各种消息结果(Message)做处理。
有不明白问我。

服务端基本架构完成，正在完成具体的编码。有些需求不是很明确，需要再跟对方交流一下。